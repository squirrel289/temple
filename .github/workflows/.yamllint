---
extends: default

# Per-folder yamllint config for GitHub workflow files.
# This file extends the repository-level `.yamllint` at the project root
# to allow folder-specific overrides if needed.

rules:
  line-length: disable
  truthy: disable
