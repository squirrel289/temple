<html>
  <head><title>User</title></head>
  <body>
    {% include 'header.html' %}
    <div id="profile">
      <h1><!-- name missing on purpose --></h1>
      <p>Age: {{ user.age }}</p>
      <p>Active: {% if user.active %}Yes{% else %}No{% endif %}</p>
      <ul>
        {% for s in user.skills %}
        <li>{{ s }}</li>
        {% endfor %}
      </ul>
      <p>Jobs:</p>
      <ul>
        {% for job in user.jobs %}
        <li>{{ job.title }} at {{ job.company }}</li>
        {% endfor %}
      </ul>
    </div>
    {% include 'footer.html' %}
  </body>
</html>

