
name = "{{ user.name }}"
age = {{ user.age }}
active = {% if user.active %}true{% else %}false{% end %}
skills = [ "{{ user.skills.0 }}"{% if user.skills.1 %}, "{{ user.skills.1 }}"{% end %} ]

{% include 'header.toml' %}

{% for job in user.jobs %}
[[jobs]]
title = "{{ job.title }}"
company = "{{ job.company }}"
{% if loop.last %}
# last job marker
{% end %}
{% end %}

{% include 'footer.toml' %}


