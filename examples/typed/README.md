# Examples for Typed/Structured Template AST Spike

This folder contains small programmatic examples that construct the prototype AST and run the simple renderer/serializers.

Run the example:

```bash
python examples/typed/run_example.py
```
